"""Training surface for OUR own reconstruction models (depth + pose), on real datasets, using our GPU.
Model-agnostic: a trained checkpoint is loaded by model/own_engine.py behind the Reconstructor contract."""
